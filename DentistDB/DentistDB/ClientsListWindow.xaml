<Window x:Class="DentistDB.ClientsListWindow" x:Name="ClientsWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:local="clr-namespace:DentistDB"
        Title="ClientsListWindow" Height="600"  MinWidth="200" Background="Aqua" ScrollViewer.CanContentScroll="True" ScrollViewer.VerticalScrollBarVisibility="Auto">
    <Window.Resources>
        <local:ImageFmStringConverter x:Key="ImageFmStringConverter"/>
        <local:HeightValueConverter x:Key="HeightValueConverter"/>
    </Window.Resources>
    <StackPanel>
        <StackPanel Orientation="Horizontal">
            <Image Source="search.jfif" Width="30" Height="20"/>
            <TextBox Name="SearchTB" MinWidth="100" TextChanged="SearchTB_TextChanged" KeyUp="SearchTB_KeyUp"/>
        </StackPanel>
        <ListView Name="ClientsLVs" ItemsSource="{Binding}" MouseDoubleClick="ClientsLVs_MouseDoubleClick" KeyUp="ClientsLVs_KeyUp" ToolTip="Press 'Del' to Delete selected Client." ScrollViewer.CanContentScroll="True" ScrollViewer.VerticalScrollBarVisibility="Visible" Height="{Binding Path=ActualHeight,ElementName=ClientsWindow,UpdateSourceTrigger=PropertyChanged,Converter={StaticResource HeightValueConverter},ConverterParameter='0.85'}">
            <ListView.ItemContainerStyle>
                <Style TargetType="{x:Type ListViewItem}">
                    <Setter Property="BorderBrush" Value="DarkBlue" />
                    <Setter Property="BorderThickness" Value="0,0,0,4" />
                </Style>
            </ListView.ItemContainerStyle>
            <ListView.ItemTemplate>
                <DataTemplate>
                    <StackPanel Orientation="Horizontal" Margin="5,0">
                        <Image Source="{Binding Image,Mode=OneWay,Converter={StaticResource ImageFmStringConverter}}" Height="60"/>
                        <StackPanel Orientation="Vertical">
                            <TextBlock Text="{Binding FirstName}" Height="20"/>
                            <TextBlock Text="{Binding SecondName}" Height="20"/>
                            <TextBlock Text="{Binding LastName}" Height="20"/>
                        </StackPanel>
                        <StackPanel Orientation="Vertical" Margin="5,0">
                            <TextBlock Text="{Binding Phone1}" Height="20"/>
                            <TextBlock Text="{Binding Phone2}" Height="20"/>
                            <TextBlock Text="{Binding Email}" Height="20"/>
                        </StackPanel>
                        <StackPanel Orientation="Vertical" Margin="5,0">
                            <Label Content="{Binding TypeID}" Height="15"/>
                            <TextBlock Text="{Binding PersonalIdNumber}" Height="15"/>
                            <Label Content="Last Update" Height="15"/>
                            <TextBlock Text="{Binding LastDateUpdate}" Height="15"/>
                        </StackPanel>
                    </StackPanel>
                </DataTemplate>
            </ListView.ItemTemplate>
        </ListView>
    </StackPanel>
</Window>
