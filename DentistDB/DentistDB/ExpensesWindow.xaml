<Window x:Class="DentistDB.ExpensesWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:DentistDB"
        mc:Ignorable="d"
        Title="ExpensesWindow" SizeToContent="WidthAndHeight" ScrollViewer.CanContentScroll="True" ScrollViewer.HorizontalScrollBarVisibility="Visible" ScrollViewer.VerticalScrollBarVisibility="Visible" MaxWidth="700" MaxHeight="500" Background="Aqua">
    <ScrollViewer MaxHeight="500" MaxWidth="700" ScrollViewer.VerticalScrollBarVisibility="Auto" ScrollViewer.CanContentScroll="True" HorizontalScrollBarVisibility="Auto">
    <StackPanel ScrollViewer.CanContentScroll="True" ScrollViewer.VerticalScrollBarVisibility="Visible" MaxHeight="500" MaxWidth="700">
            <StackPanel Orientation="Horizontal">
                <Label Content="От:" Target="{Binding ElementName=FromDP}" FontWeight="Bold"/>
                <DatePicker Name="FromDP" SelectedDateChanged="FromDP_SelectedDateChanged"/>
                <Label Content="До:" Target="{Binding ElementName=ToDP}" FontWeight="Bold"/>
                <DatePicker Name="ToDP" SelectedDateChanged="FromDP_SelectedDateChanged"/>
            </StackPanel>
            <Button x:Name="ExpencesBtn" Content="Добави Разход" Click="NewExpence_Click"/>
        <DataGrid Name="InvoicesDG" ItemsSource="{Binding}" AutoGenerateColumns="False" CanUserAddRows="False" CanUserDeleteRows="True" PreviewKeyDown="InvoicesDG_PreviewKeyDown"  MouseDoubleClick="InvoicesDG_MouseDoubleClick">
            <DataGrid.Columns>
                <DataGridTextColumn Header="Фирма" Binding="{Binding SupplierName,Mode=OneWay}"/>
                <DataGridTextColumn Header="Номер" Binding="{Binding InvoiceNumber,Mode=OneWay}"/>
                <DataGridTextColumn Header="Дата" Binding="{Binding DateOfPayment,Mode=OneWay, StringFormat=\{0:dd.MMM.yy\}}"/>
                <DataGridCheckBoxColumn Header="В брой" Binding="{Binding IsCashPayment, Mode=OneWay}"/>
                <DataGridTextColumn Header="Сума" Binding="{Binding Sum,Mode=OneWay, StringFormat=\{0:N2\}}"/>
                <DataGridTextColumn Header="#" Binding="{Binding Currency, Mode=OneWay}"/>
            </DataGrid.Columns>
        </DataGrid>
    </StackPanel>
    </ScrollViewer>
</Window>
