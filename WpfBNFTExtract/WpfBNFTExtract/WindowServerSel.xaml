<Window x:Class="WpfBNFTExtract.WindowServerSel"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="WindowServerSel" Height="300" Width="300">
    <StackPanel Orientation="Vertical">        
        <DataGrid Name="ServersGrid" ItemsSource="{Binding}" SelectionMode="Single" SelectionUnit="FullRow">
            <DataGrid.Columns>
                <DataGridTextColumn Header="ServerName" Binding="{Binding ServerName}"/>
                <DataGridTextColumn Header="Instance" Binding="{Binding InstanceName}"/>
                <DataGridTextColumn Header="Version" Binding="{Binding Version}"/>
            </DataGrid.Columns>
        </DataGrid>
        <StackPanel Orientation="Horizontal">
            <Button Content="Select" Click="ButtonSelect_Click" Margin="5"/>
            <Button Content="Cancel" Click="ButtonCancel_Click" Margin="5"/>
        </StackPanel>
    </StackPanel>

</Window>